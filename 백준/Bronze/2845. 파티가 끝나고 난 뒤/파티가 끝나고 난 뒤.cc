#include<iostream>
using namespace std;

int main(void)
{
    int Lee, jung, wu, a1, a2, a3, a4, a5;

    cin >> Lee >> jung;

    wu = Lee * jung;

    cin >> a1 >> a2 >> a3 >> a4 >> a5;

    cout << a1 - wu << " " << a2 - wu << " " << a3 - wu << " " << a4 - wu << " " << a5 - wu << endl;

    return 0;
}
