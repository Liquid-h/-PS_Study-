#include<iostream>
using namespace std;

int main(void)
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);

	int n;

	cin >> n;
	int result = 0;

	result = n / 10;

	cout << result << endl;

	return 0;
}