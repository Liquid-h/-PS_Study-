#include <iostream>
using namespace std;

int main(void) 
{
    ios::sync_with_stdio(false);
     cin.tie(NULL);

    int a,b,c,d;

    cin >> a>>b>>c>>d;

    cout << (a*b)+(c*d);

    return 0;
}